{
  "name": "@weelink/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "ts-node -e \"require('dotenv').config({ path: '../../.env' })\" prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "prisma": "^6.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "dotenv": "^16.4.7",
    "@types/bcrypt": "^5.0.2"
  }
}
