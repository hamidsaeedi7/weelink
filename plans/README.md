# Weelink improvement plans

Generated by a read-only `/improve` audit (4 parallel category sweeps: correctness/security,
performance/architecture, test coverage/DX/docs, dependencies/migrations/direction), personally
vetted finding-by-finding against the actual source before any plan was written. Every plan below
is self-contained — written for a separate executor with no memory of the audit conversation.

**Written against commit:** `6afe742`. Each plan re-states this; if the working tree has moved on
significantly before a plan is executed, re-verify the plan's "current state" excerpts against the
live file before trusting them (drift check).

**Audit scope disclosure:** this was a "standard" effort audit (hotspot-weighted, ≤4 concurrent
subagents). `apps/mobile` (Flutter) and a field-by-field review of the full Prisma schema were not
covered. The skill's reference files (`references/audit-playbook.md`,
`references/plan-template.md`) were not present in the installed skill package on this machine —
plans follow the structure specified inline in `SKILL.md` instead.

**Selection:** these 12 plans are the subset the user selected from the full audit findings table
(24 findings + 4 direction suggestions) — the cheapest, lowest-risk, highest-confidence fixes,
plus two higher-effort performance items the user judged worth doing now. Findings not selected
(major dependency bumps, Prisma migration-history retrofit, `pnpm lint` config, git hooks, refresh
token rotation, appointment race condition, admin XSS sanitization, unbounded queries, raw `<img>`
usage, blanket `"use client"`) were deliberately deferred — see the audit conversation for the
reasoning per item, not repeated here.

**Execution status:** all 12 plans below have been executed, type-checked, built, and
live-verified in production (health endpoint responding, Redis cache key confirmed, upload
cache-control header confirmed, boot guard passed on restart, new API routes mapped).

## Priority order & status

| # | Plan | Category | Impact | Effort | Status |
|---|------|----------|--------|--------|--------|
| 001 | [Rotate admin credentials](001-rotate-admin-credentials.md) | Security | High | S | DONE |
| 002 | [Fix custom order price manipulation](002-fix-custom-order-price.md) | Money | Med-High | S | DONE |
| 003 | [Remove deploy.sh accept-data-loss fallback](003-remove-deploy-accept-data-loss.md) | Migrations/Deploy | High | S | DONE |
| 004 | [Add /health endpoint](004-add-health-endpoint.md) | Ops | Med-High | S | DONE |
| 005 | [Remove SVG from upload allowlist](005-remove-svg-upload.md) | Security | Medium | S | DONE |
| 006 | [Fix daily-stats bucketing](006-fix-daily-stats-bucketing.md) | Performance | Med-High | S | DONE |
| 007 | [Per-phone OTP rate limit](007-otp-per-phone-rate-limit.md) | Abuse | Medium | S | DONE |
| 008 | [Complete .env.example](008-env-example-and-boot-guard.md) | Docs | Medium | S | DONE |
| 009 | [Backend localhost-fallback boot guard](009-backend-localhost-fallback-guard.md) | Env safety | High | S | DONE |
| 010 | [Upload cache-control headers](010-upload-cache-control-headers.md) | Performance | Medium | S | DONE |
| 011 | [Redis cache for shop-by-slug](011-redis-cache-shop-by-slug.md) | Performance | High | M | DONE |
| 012 | [Bio-page server-side storefront data](012-bio-page-server-side-storefront-data.md) | Performance | High | M | DONE |
| 013 | [Fix robots.ts sitemap domain](013-fix-robots-sitemap-domain.md) | SEO | High | S | DONE |
| 014 | [Fix backup.sh (uploads + prod)](014-fix-backup-script.md) | Backup | High | S | DONE |
| 015 | [Avatar/banner `priority` (LCP)](015-image-priority-lcp.md) | Performance | High | S | DONE |
| 016 | [Missing DB indexes (Order, Coupon)](016-missing-db-indexes.md) | Database | Medium | S | DONE |
| 017 | [Dashboard noindex metadata](017-dashboard-noindex.md) | SEO | Medium | S | DONE |
| 018 | [Docker hardening](018-docker-hardening.md) | Docker | Medium | M | DONE (Steps 1-3; Step 4 skipped) |
| 019 | [Compose resource limits + healthchecks](019-compose-resource-limits-healthchecks.md) | Ops | High | M | DONE — see incident notes in the plan |
| 020 | [SEO metadata for course/file pages](020-seo-metadata-course-file-pages.md) | SEO | High | M | DONE |

### Second audit pass (013-020)

Generated by a follow-up `/improve` pass covering categories the first pass didn't audit:
accessibility, SEO, Core Web Vitals, Docker/CI/DevOps, database indexes, backup/rollback. Two
parallel Explore agents (a11y/SEO/CWV; Docker/DevOps/DB-indexes), findings personally re-verified
against source before this index was written. 27 findings total; the 8 above are the ones the user
selected as either cheap-and-real ("همین الان بکن") or worth the larger effort ("ارزش داره") —
see the conversation for the full findings table and the reasoning behind what was deferred
(a full accessibility overhaul — labels, modal focus-trap/ARIA — and multi-replica scalability
work — Redis-backed rate limiting, object-storage uploads — were judged premature for a
single-VPS, single-replica deployment at current scale).

## Dependency graph

Almost all plans are independent and can be executed in any order. The two exceptions:

- **008 → nothing depends on it**, but its `.env.example` additions (`API_URL`, `WEB_URL`) document
  the same vars that **009** adds a boot-time hard requirement for. Recommend doing 008 before 009
  (docs before enforcement), though 009 does not technically require 008 to be merged first.
- **011 and 012 both edit `ShopsService.findBySlug`** in the same file
  (`apps/api/src/shops/shops.service.ts`). They are functionally independent (011 adds caching, 012
  adds new response fields) but will conflict if executed as parallel diffs against the same base
  commit. **Recommend executing 011 first, then 012** — 012's plan explicitly checks for and builds
  on top of 011's changes if present. If executing in the opposite order, 011's plan will need the
  same adjustment in reverse (cache the extra fields 012 adds too) — its plan doesn't currently
  assume 012 landed first, so re-verify before running 011 if 012 already merged.

Every other plan (001-007, 009, 010) touches disjoint files and can run in parallel with any other.

Among the second-pass plans (013-020): all 8 touch disjoint files and are independent of each
other and of 001-012, **except** plan 018 (Docker hardening) should land before plan 019 (compose
resource limits/healthchecks) is deployed if both are done in the same session — not because they
conflict textually (different files), but because 018's Step 4 (optional multi-stage rebuild of
`Dockerfile.api`) changes the image-build process, and it's cleaner to confirm that's stable before
also changing the compose file's runtime resource ceilings in the same deploy window. Not a hard
blocker — just a recommended ordering to isolate variables if something goes wrong after deploy.

## Rotation / manual-action note

Plan 001 includes a manual credential-rotation step (Step 1) that requires checking live production
data and is explicitly marked "STOP and report back" if the executor isn't certain which account
to rotate — don't let an executor agent guess on this one.

Plan 018's Step 4 (optional multi-stage `Dockerfile.api` split) and plan 019's memory-limit sizing
both carry "stop and report back" escape hatches for production-affecting edge cases (file
permission errors after switching to a non-root user; a container hitting its new memory ceiling)
— don't let an executor push through those blind either.

## Considered and rejected (do not re-audit these on the next `/improve` run)

None of the 20 audited findings across both passes were rejected during vetting — all survived
personal verification against the source. The findings NOT selected for a plan (see "Selection"
above, and the second-pass note) were deferred by user choice, not rejected as invalid; they remain
valid candidates for a future plan-writing pass and don't need to be re-discovered by a future
audit, just re-considered for selection.
